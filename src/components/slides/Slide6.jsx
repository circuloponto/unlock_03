import React from 'react';
import SlideSection from './SlideSection';

const Slide6 = ({ getSlideColor }) => {
  return (
    <SlideSection $bgColor={getSlideColor(5, 0)}>
      {/* Slide 6 content */}
    </SlideSection>
  );
};

export default Slide6;
